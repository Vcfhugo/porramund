node_modules/
.DS_Store
.env
.env.local
.env.*.local
# supabase-client.js — se commitea: la anon key es pública (la seguridad la dan las RLS).
#                      Necesario porque el deploy estático no tiene paso de build.
.netlify/state.json
supabase/.temp/
